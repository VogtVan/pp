---
slug: {slug}
kind: plan
status: todo
created: {created}
constraints.production: |
  {blueprint}
---

# PLAN `{slug}`

## Context

{prose}

## Target

What the undertaking becomes and where it stops: the direction settled, the shape it takes, what stays out. *e.g. the tool keeps its notes in one JSON-lines file behind a command line; a web front stays out.*

## Expected

What is TRUE when the plan closes, each line with the rendering that shows it. *e.g. `memo add` then `memo list` prints the note back — the bench scenario cited by name.*

## Arbitrations

The open questions of the framing: the options, the recommendation, what returns to the operator's GO. *e.g. JSON lines or SQLite — reco JSON lines: one file, no dependency.*

## Items

The phases prepared, one line each `- slug -- brief`, the brief saying what the phase is about, its exit and its proof in a sentence; the operator amends a line until « frame » names it. *e.g. `- core -- the store and the two commands; exit: the bench green on the store`*
