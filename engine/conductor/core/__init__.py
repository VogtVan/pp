"""core -- the DATA layer's model: what a run is made of (the model, its language,
its errors, its version, the deviation and the navigation over a stack). Nothing
here imports a process module: the layer above reads it, never the reverse."""
