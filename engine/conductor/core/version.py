"""The PRODUCT's version -- declared here, mirrored by pyproject, released as a tag.

Beta while the work converges (`0.1.0-beta.N`, bumped on engine-visible change --
the operator's GO of 2026-08-23 closed the alpha at 100); the release call
(R6) sets the plain `0.1.0` and tags. `instance.yaml`
records the copy an instance runs on; `upgrade` compares and moves it.
"""
VERSION = "0.1.0-beta.11"
