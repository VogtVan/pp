"""packaging -- what compiles, installs and signals: the catalog and the laws
compiled from the vendored bundles (compiling), the instance seeded, synced and
doctored (install), the bus of events the packages declare (events), the
contract of the packages' contributions, validated at the build (contributions).
A process layer beside execution: it reads below, never the facade."""
