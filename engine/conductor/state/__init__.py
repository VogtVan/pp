"""state -- the DATA layer's keeping: what is held between two calls (the run's
slot and its trace, the records, the metrics, the settings, the instance and
the discovery of members). It imports core alone; a process module feeds it
what it cannot read itself."""
