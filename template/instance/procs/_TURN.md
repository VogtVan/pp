---
name: TURN
readme: |
  An OVERLAY example, inert while the file name starts with `_` (a `_` file is
  never scanned). Arm it by renaming to `TURN.md`: its deltas then merge over
  the kit's TURN -- `-T4` lifts "verify instead of taking for granted", `T6` puts your
  own law in force. The bundle itself stays untouched.
constraints.production: |
  T6  answers stay under ten lines unless asked otherwise.
  -T4
---
